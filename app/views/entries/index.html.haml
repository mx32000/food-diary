%h1 Your Entries

%table
  %thead
    %tr
      %th Food
      %th Quantity
      %th Consumed At
      %th Actions
  %tbody
    - @entries.reverse.each do |entry|
      = render entry

= link_to 'New Entry', new_entry_path